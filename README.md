# Larao Emacs Config
## Manjaro(xfce)
```
sudo pacman -S emacs-nativecomp
sudo pacman -S cmake
paru -S emacs-lsp-booster

mkdir ~/Documents/org/
touch ~/Documents/tasks.org
mkdir ~/Documents/org/roam/

git clone https://github.com/laraochan/emacs-config ~/.emacs.d
```
